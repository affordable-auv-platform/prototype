# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(remote_controlTaskLib)
ADD_LIBRARY(${REMOTE_CONTROL_TASKLIB_NAME} SHARED 
    ${REMOTE_CONTROL_TASKLIB_SOURCES})
add_dependencies(${REMOTE_CONTROL_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${REMOTE_CONTROL_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${REMOTE_CONTROL_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${REMOTE_CONTROL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${REMOTE_CONTROL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${REMOTE_CONTROL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${REMOTE_CONTROL_TASKLIB_HEADERS}
    DESTINATION include/orocos/remote_control)

