# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(udp_serialTaskLib)


ADD_LIBRARY(${UDP_SERIAL_TASKLIB_NAME} SHARED 
    ${UDP_SERIAL_TASKLIB_SOURCES})
add_dependencies(${UDP_SERIAL_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${UDP_SERIAL_TASKLIB_NAME    serial
    ${OrocosRTT_LIBRARIES}
    ${UDP_SERIAL_TASKLIB_DEPENDENT_LIBRARIES})
    
SET_TARGET_PROPERTIES(${UDP_SERIAL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${UDP_SERIAL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${UDP_SERIAL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${UDP_SERIAL_TASKLIB_HEADERS}
    DESTINATION include/orocos/udp_serial)

